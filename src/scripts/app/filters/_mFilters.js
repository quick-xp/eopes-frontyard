'use strict';

/**
 * Create services module
 */

var getModuleInstance = require('../utilities/getModuleInstance');

var mFilters = getModuleInstance('mFilters');

module.exports = mFilters;
